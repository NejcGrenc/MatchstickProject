package grenc.masters.matchsticktask.type;

/**
 * This enum refers to the initial strategy that group is given.
 * Group with strategy A will start with tasks solvable by A and slowly progress to tasks solvable by B, and vice versa.
 */
public enum MatchstickGroupInitialStrategy
{
	STRATEGY_A,
	STRATEGY_B;
}
